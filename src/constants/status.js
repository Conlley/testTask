export const DEFAULT = (message = '') => ({ value: 'DEFAULT', message });
export const LOADING = (message = 'Loading...') => ({ value: 'LOADING', message });
export const SUCCESS = (message = '') => ({ value: 'SUCCESS', message });
export const FAILURE = (message = 'Some error happened.') => ({ value: 'FAILURE', message });
